# landing-page — instructions for Codex and other agents

This repo lives inside the InaiSec workspace folder `IRS/` (the parent directory), which is not itself a git repo.

1. Read `../CLAUDE.md` (workspace-level context, constraints, review gate) and `../AGENTS.md` (workspace rules for Codex) first.
2. Then read this repo's own `CLAUDE.md` if present — it is the component-specific instruction file and takes precedence for work inside this repo.
3. Run `git` only inside this repo. Never commit or push unless the owner explicitly asks.
